# dependencies (incl. the symlinked OpenCode plugin deps)
node_modules/

# build output
dist/
.astro/

# caches
.cache/

# logs / OS cruft
*.log
.DS_Store

# The shipped CLI is still zero-dependency — bin/ imports nothing but node: —
# but the repo now has devDependencies (changesets), so the root lockfile is
# tracked. CI installs with --frozen-lockfile and needs it. site/ keeps its own.
