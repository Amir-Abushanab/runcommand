# dependencies (incl. the symlinked OpenCode plugin deps)
node_modules/

# build output
dist/
.astro/

# caches
.cache/

# logs / OS cruft
*.log
.DS_Store

# stray root lockfile (this package is zero-dep; site/ keeps its own)
/pnpm-lock.yaml
